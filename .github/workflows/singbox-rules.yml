name: Build sing‑box rule‑set

# 触发方式：每日定时、push 到 main、手动触发
on:
  schedule:
    - cron: '0 3 * * *'   # 每天 03:00 UTC
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # 需要写权限才能 push

    steps:
      # -------------------------------------------------
      # 1️⃣ 拉取本仓库代码（只需要本仓库）
      # -------------------------------------------------
      - name: Checkout this repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -------------------------------------------------
      # 2️⃣ 下载最新的 geosite（dlc.dat）和 geoip.dat
      # -------------------------------------------------
      - name: Download latest dlc.dat (geosite)
        run: |
          curl -L -o dlc.dat \
            https://github.com/v2fly/domain-list-community/releases/latest/download/dlc.dat

      - name: Download latest geoip.dat
        run: |
          curl -L -o geoip.dat \
            https://github.com/v2fly/geoip/releases/latest/download/geoip.dat

      # -------------------------------------------------
      # 3️⃣ Checkout meta‑rules‑converter 并编译
      # -------------------------------------------------
      - name: Checkout meta‑rules‑converter
        uses: actions/checkout@v4
        with:
          repository: MetaCubeX/meta-rules-converter
          path: converter

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'   # 当前稳定版

      - name: Build converter binary
        working-directory: converter
        run: |
          go build -o converter .   # 直接在根目录编译

      # -------------------------------------------------
      # 4️⃣ 执行转换（生成 sing‑box rule‑set）
      # -------------------------------------------------
      - name: Convert geosite → sing‑box
        env:
          CONV: ${{ github.workspace }}/converter/converter
        run: |
          mkdir -p sing-rule/geo/geosite
          $CONV geosite -in dlc.dat -out sing-box -outDir ./sing-rule/geo/geosite

      - name: Convert geoip → sing‑box
        env:
          CONV: ${{ github.workspace }}/converter/converter
        run: |
          mkdir -p sing-rule/geo/geoip
          $CONV geoiP -in geoip.dat -out sing-box -outDir ./sing-rule/geo/geoip

      # -------------------------------------------------
      # 5️⃣ 提交并推送生成的 rule‑set（如果有改动）
      # -------------------------------------------------
      - name: Commit generated rule‑set
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sing-rule/
          if ! git diff --cached --quiet; then
            git commit -m "🤖 Update sing‑box rule‑set (auto)"
            git push
          else
            echo "No changes, skip commit."
          fi

      # -------------------------------------------------
      # 6️⃣（可选）发布 Release 并上传资产
      # -------------------------------------------------
      - name: Create Release (optional)
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "sing-rule-$(date +%Y%m%d%H%M)"
          name: "sing‑box rule‑set $(date +%Y-%m-%d %H:%M)"
          body: |
            自动生成的 sing‑box geosite / geoip rule‑set，来源于 v2fly/domain‑list‑community 与官方 geoip。
          files: |
            sing-rule/geo/geosite/*
            sing-rule/geo/geoip/*
